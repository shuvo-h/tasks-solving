type THasOrNotHasPieGraphProps = {
    filteredData: any[];
  };

const HasOrNotHasPieGraph = ({filteredData}:THasOrNotHasPieGraphProps) => {
    console.log(filteredData);
    
    return (
        <div>
            HasOrNotHasPieGraph
        </div>
    );
};

export default HasOrNotHasPieGraph;